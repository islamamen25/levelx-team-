# خطوات رفع منتج عن طريق Cowork

> دليل عملي لإنت (اللي هتتعامل مع cowork)، مش للـ agent نفسه — هو بيقرا `COWORK.md`
> لوحده. الملف ده بيقولك إنت هتعمل إيه خطوة بخطوة، وإزاي تتأكد إن كل حاجة نجحت.

---

## قبل ما تبدأ — جهّز 3 حاجات

### 1. صور المنتج
مربّعة، 1500×1500 بكسل تقريباً، أقل من 500 كيلوبايت. التفاصيل كاملة في
[`IMAGE-GUIDE.md`](IMAGE-GUIDE.md).

### 2. بيانات المنتج الأساسية
| الحقل | مطلوب؟ | مثال |
|---|---|---|
| الاسم (عربي) | ✅ إلزامي | "سلك شحن 3 في 1" |
| الاسم (إنجليزي) | اختياري لكن يُفضَّل | "3-in-1 Charging Cable" |
| الوصف | اختياري | جملتين-تلاتة عن المنتج |
| الماركة | اختياري | "Anker" |
| السعر | ✅ إلزامي | 1300 |
| سعر بعد الخصم (لو فيه عرض) | اختياري | 999 |
| الحالة | ✅ إلزامي | ⚠️ شوف الملاحظة تحت الجدول |
| الكمية المتوفرة | اختياري (افتراضياً 0) | 25 |
| القسم اللي هيتحط فيه | ✅ إلزامي عملياً | "إكسسوارات الموبايل" |

> ⚠️ **ملاحظة عن "الحالة" (`condition`):** الخيارات المتاحة تقنياً هي
> `Premium`/`Excellent`/`Good`/`Fair` — أسامي باقية من وقت ما المتجر كان بيبيع
> منتجات مجدّدة (اتشالت الفكرة دي من الموقع بالكامل). دلوقتي المتجر بيبيع جديد،
> فمفيش قيمة "صحيحة" واضحة من الأربعة دول تناسب "منتج جديد" بشكل مباشر —
> المنتجات الموجودة حالياً في قاعدة البيانات كلها متسجّلة بـ`Good` (وهي القيمة
> الافتراضية لو محدّدتش حاجة). **قرار إنت تاخده:** تفضل مستخدم `Good` زي
> الموجود عشان التناسق، ولا تحب تغيّر الأربع خيارات دي لحاجة تناسب "متجر جديد"
> أصلاً (زي إزالتها خالص من الموقع)؟ لو عايز رأيي — الأنضف إنك تسيبها `Good`
> دلوقتي عشان التناسق مع اللي موجود، وتفكّر في تعديل الحقل ده كمشروع منفصل بعدين.

### 3. تأكيد القسم موجود
افتح [`COWORK.md`](COWORK.md) قسم "`categories`" وشوف لو القسم اللي محتاجه
موجود في الـ17 قسم الحاليين. لو مش موجود، قوله لـ cowork يضيفه **مع الاسم
العربي** (`name_ar`) — تفاصيل كاملة في نفس القسم بتاع الملف.

---

## الخطوات

### الخطوة 1 — ابعت البيانات لـ Cowork
ابعتله صورة/جدول المنتجات (زي ما بتعمل عادةً)، ومعاها روابط الصور أو الصور
نفسها. Cowork هيقرا `COWORK.md` لوحده ويعرف إزاي يكتبها صح.

### الخطوة 2 — Cowork بيكتب على قاعدة البيانات
هو بيعمل الترتيب ده لوحده (من `COWORK.md` قسم 7):
1. يحفظ بيانات المنتج الأساسية
2. يحفظ السعر والحالة والكمية (`variants`) — **لازم صف واحد على الأقل وإلا
   المنتج مش هيظهر في أي قائمة**
3. يحفظ الترجمة العربي
4. يحفظ الترجمة الإنجليزي (لو موجودة)

### الخطوة 3 — ⚠️ خطوة إلزامية: تحديث الموقع
لازم Cowork ينده على:
```bash
curl -X POST "$SITE_URL/api/revalidate" \
  -H "x-webhook-secret: $REVALIDATE_SECRET" \
  -H "Content-Type: application/json" \
  -d '{}'
```
**من غيرها المنتج مش هيبان على الموقع لحد ساعة كاملة**، حتى لو اتسجّل صح في
قاعدة البيانات. لو رفعت دفعة منتجات مع بعض، نداء واحد في الآخر كفاية.

### الخطوة 4 — تأكّد بنفسك
افتح رابط المنتج على الموقع الحي وشوفه بعينك — دي الخطوة اللي مفيش بديل ليها:

- `https://levelx-team.vercel.app/ar/products/<slug-بتاع-المنتج>`
- وكمان افتح صفحة القسم اللي المفروض يبان فيه، وتأكد إنه ظاهر في القائمة
  مش بس في صفحته الخاصة

---

## لو المنتج ما ظهرش — إتفحص الأسباب دي بالترتيب

من `COWORK.md` قسم 3 — دي أشهر 4 أسباب:

| السبب | إزاي تتأكد |
|---|---|
| نسيت خطوة الـ revalidate | ادّي دقيقة وحاول تاني، ولو لسه مش ظاهر، اطلب من cowork يبعت النداء تاني |
| المنتج متسجّلش له `variant` (سعر/حالة) | صفحة المنتج بتفتح لوحدها بس مش بيبان في القوائم — ده أشهر غلطة |
| القسم (`category_id`) غلط أو فاضي | المنتج موجود بس مش هيبان تحت أي قسم |
| `is_active` مطفي | المنتج مخفي بالكامل، مش هتلاقيه حتى برابطه المباشر |

---

## خلاصة سريعة (Cheat Sheet)

```
1. جهّز الصور (مربّعة، 1500×1500، أقل من 500KB)
2. جهّز البيانات (اسم عربي إلزامي، سعر إلزامي، قسم إلزامي)
3. تأكّد القسم موجود — لو جديد، لازم name_ar
4. ابعت لـ Cowork
5. اتأكّد إنه بعت /api/revalidate  ← أشهر خطوة بتتنسى
6. افتح رابط المنتج بنفسك وشوفه
```

---

*آخر تحديث: 2026-07-27 — الخطوات دي مبنية على العقد الموثّق في `COWORK.md`
(اللي بيحكم فعلياً إزاي Cowork بيكتب على قاعدة البيانات).*
